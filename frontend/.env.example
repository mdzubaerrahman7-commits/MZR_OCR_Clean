# Server-side only (never sent to the browser) — read at request time by the
# Next.js rewrite proxy in next.config.mjs, which forwards every /api/* request
# from the browser to this address. Defaults to http://localhost:8000 if unset.
INTERNAL_API_URL=http://localhost:8000
